---
import { ChevronRight } from 'lucide-astro';

interface Props {
  href: string;
  title: string;
  subtitle?: string;
  isExternal?: boolean;
}

const { href, title, subtitle, isExternal = false } = Astro.props;
---

<a 
  href={href}
  target={isExternal ? "_blank" : "_self"}
  rel={isExternal ? "noopener noreferrer" : ""}
  class="link-card group block bg-zinc-900/90 border border-zinc-800 rounded-2xl p-5 shadow-lg active:scale-[0.97] active:bg-zinc-800/90 transition-all duration-150"
>
  <div class="flex items-center justify-between">
    <div class="flex flex-col gap-1">
      <h2 class="text-xl font-semibold text-zinc-100 group-hover:text-amber-500 transition-colors">
        {title}
      </h2>
      {subtitle && (
        <span class="text-sm text-zinc-400">
          {subtitle}
        </span>
      )}
    </div>
    
    <div class="text-zinc-500 group-hover:text-amber-500 transition-colors">
      <ChevronRight size={24} />
    </div>
  </div>
</a>
